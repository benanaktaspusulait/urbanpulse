syntax = "proto3";

package urbanpulse;

message AlertNotification {
    string alertId = 1;
    string commandId = 2;
    string type = 3; // e.g., "email", "sms", "webhook"
    string message = 4;
    int64 timestamp = 5;
}